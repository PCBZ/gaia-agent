# LlamaIndex core + Gemini LLM
llama-index-core>=0.14.0
llama-index-llms-google-genai>=0.10.0
llama-index-llms-openai-like>=0.3.0   # HF Inference Providers (huggingface_llm)

# Tools
llama-index-tools-code-interpreter>=0.4.0
ddgs>=9.0            # web_search (multi-backend fallback, keyless)
pandas>=2.0          # read_tables (pd.read_html) + xlsx questions
lxml>=5.0            # pd.read_html backend
beautifulsoup4>=4.12 # pd.read_html fallback backend
youtube-transcript-api>=1.0   # q7: fetch YouTube captions (no video download)
# read_url uses Jina Reader (https://r.jina.ai) over plain requests — no package.

# Data / API / config
pydantic>=2.0
requests>=2.31
python-dotenv>=1.0
huggingface_hub>=0.24
