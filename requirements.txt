# Pinned to versions verified to co-exist locally, so pip does not backtrack
# during the (slow) HF Space build. sdk_version in README.md must match gradio.

# LlamaIndex core + LLMs
llama-index-core==0.14.24
llama-index-llms-google-genai==0.10.0
llama-index-llms-openai-like==0.7.2

# Tools
llama-index-tools-code-interpreter==0.5.0
ddgs==9.15.0
pandas>=2.2,<3      # HF Space runs Python 3.10; pandas 3.x needs 3.11+
lxml>=5            # 3.10-compatible
beautifulsoup4==4.15.0
youtube-transcript-api==1.2.4

# App (HF Space) — must equal README sdk_version
gradio==6.26.0

# Data / API / config
pydantic==2.13.4
httpx==0.28.1
python-dotenv==1.2.3
huggingface_hub>=0.24
